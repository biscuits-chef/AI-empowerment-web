# 第一阶段前端需求基线

## 业务目标

为公司内部用户提供智能问答工作台。统一工作台交互以[飞书交互原型](https://my.feishu.cn/wiki/HC7Dwym3Vi2jbikwnq6cMqhhnUf?from=from_wechat)、已归档的《产品需求文档》及后续确认结论为基线。用户提问后，后端分别查询公司知识库平台和业务数据库，整合证据并调用公司大模型。两个通道数据不一致时，前端必须展示冲突提示、保留双方信息并要求人工复核。

## 问答范围

1. 产品及交易基础信息：产品经理、投资经理、产品形式、起止日期、费率、单笔交易对应交易员等。
2. 产品最新信息：最新产品说明书、费率调整公告、产品备案通知书的关键要素，以及产品分层和费率调整计划。

## 功能需求

- 页面采用统一工作台布局：左侧固定展示品牌区、新建会话、会话搜索、按日期分组的历史会话和当前用户；页面首次打开时左侧会话列表默认展开，并与右侧主工作区占据两个独立布局列，任何宽度下侧栏都不得以浮层覆盖右侧内容。用户可以折叠侧栏，折叠后右侧主工作区占用释放出的空间，并可通过顶部按钮重新展开；窄屏展开时仅压缩侧栏宽度。Agent 仅在输入区下拉菜单选择，侧栏不重复展示 Agent 目录。
- 统一工作台品牌名称为“智浦小鹿”，页面标题和欢迎区统一展示“智浦小鹿工作台”；仅在新建会话首次提问前，输入区底部工具栏使用带简笔画图标、无外边框且不显示箭头的轻量 Agent 选择器。点击选择器后展示与工作台视觉一致的能力菜单，支持重复点击、点击空白处和退出键关闭；第一阶段默认且仅开放“智能问数”。进入已有会话后隐藏选择器且不允许修改 Agent。
- 页面提供 Agent 选择入口，Agent 名称、稳定标识、顺序和开放状态由前端本地配置，不调用 `/api/v1/agents`。输入区一期默认且仅开放“智能问数”，类型为 `SMART_DATA`；用户选择其他未开放入口时弹出“该功能尚未开放”，不得创建会话或发送执行请求。
- 空白会话展示“今天想完成什么？”、当前 Agent、双通道能力说明、主输入框和一期范围内的快捷任务；点击快捷任务只填入输入框，不直接提交。
- 新建、删除、选择和修改聊天名称。
- 点击“新建会话”只进入空白草稿态，第一次有效提问时调用一次统一提交接口，由后端在同一事务中创建会话、问题和回答；未提问即退出不产生空会话。非首次提问使用同一接口并携带已有会话 ID，不创建新会话。
- 会话列表通过不透明稳定游标分批加载，并使用虚拟滚动，只渲染可见区和少量缓冲项；分页加载后仍保持“今天、昨天、更早”分组与服务端顺序。
- 会话修改名称和删除确认使用页面内可访问对话框，不依赖浏览器原生 `prompt` 或 `confirm`；操作菜单所在会话行和对话框必须始终位于会话标题及其悬停滚动动画之上，后续会话标题不得遮挡菜单或对话框；操作菜单和对话框支持点击外部空白区域或按 `Esc` 关闭。
- 会话存在执行中回答时禁止删除。后端返回 `409` 后，页面固定提示“会话正在执行，请停止后删除”；前端不得自动停止回答，用户停止并得到终态后才能再次删除。
- 展示聊天历史和消息历史；左侧会话根据浏览器本地自然日按“今天、昨天、更早”分组，搜索结果继续使用相同分组规则，空分组不展示。旧会话提交新问题或重新发起原问题后，应根据本次活动时间立即移动到“今天”，不等待刷新。
- 左侧会话默认标题取首次有效提问去除首尾空白后的前 100 个字符。用户主动修改名称后不得被后续提问覆盖。会话行的消息图标和操作图标使用固定尺寸，不得被任意长度的标题压缩。标题默认单行省略，鼠标悬停在被截断标题上时先短暂停留，再以每秒约 30 像素的可读速度匀速向左滚动，离开后恢复开头；系统启用减少动画时改为即时展示。顶部当前会话标题保持静态单行省略，不参与滚动，并始终为右侧环境标识和主题按钮保留固定空间。
- 新建会话输入区必须显示当前 Agent，首次提交问题时把稳定类型放入请求体的 `agentType` 字段，不得把中文名称作为接口参数；已有会话不显示 Agent 选择器，后续提问不发送 `agentType`。输入区同时支持回车发送和 `Shift + Enter` 换行。第一阶段不展示文件上传入口，也不支持拖拽文件。
- 发送问题并流式展示生成阶段和回答。执行中在输入区提供醒目的停止按钮，消息内保留同等能力，二者调用同一服务端取消流程。
- 助手消息按“执行过程、执行结果、来源与产物”分区展示：执行过程可展开并以五阶段时间线展示问题理解、知识库检索、GoldenDB 查询、双通道核验、回答生成与保存；每个阶段包含职责、等待/执行/完成/需关注/失败/停止状态、开始时间、耗时及安全结果摘要，同时展示场景、流程版本和运行编号。执行结果支持安全的标题、列表、代码和表格；来源与产物独立折叠展示。
- 白盒执行过程不得展示公司大模型系统提示词、思维链、原始 SQL、内部服务地址、密钥或未授权业务正文；查询数量、来源数量、意图类型、置信度、冲突状态等可核验摘要由服务端结构化事件提供，前端不得从回答正文猜测。
- 停止生成；停止结果必须由后端确认。
- 复制、喜欢、不喜欢和重新生成答案。
- 正常答案必须保留；点击重新生成后，原问题应作为新的用户消息再次出现，随后展示重新执行完整问答链路得到的新回答。原问题和原答案不得覆盖。
- 显示引用来源与双通道冲突提示。
- 支持上下文连续问答，具体上下文窗口和裁剪策略由后端负责。
- 2026-09-14 范围调整：第一阶段不支持文件上传、附件提问、Excel 文件输入、OCR 或 OBS 文件处理；上述能力整体顺延到后续阶段。
- 为兼容开发期已有数据，历史消息如果包含旧附件元数据可以只读展示，但一期不得创建新的附件数据或提供下载入口。
- 页面刷新或重新进入会话时，必须同时恢复回答正文、执行阶段、来源/产物、人工复核提示和真实状态；仍在执行的回答从持久事件继续跟随，不能只恢复正文或把执行态误判为失败。
- 产物预览、下载和文件类型图标以后端返回的授权产物结构为准。当前只有引用标识时只展示引用卡片，不得伪造下载地址或把引用冒充可下载产物。

## 非功能需求

- 问答提交量基线为 10 QPS，前端避免重复请求并对限流给出明确提示。
- 所有面向后端的业务调用只允许 GET 和 POST；GET 只用于读取，全部状态变更使用 POST，不得发送 PUT、PATCH、DELETE 或其他方法。每项操作使用唯一 URL，禁止通过 GET/POST 区分相同路径。
- 问题最长 4000 字，前后端都校验。
- 支持最近两个主流版本的 Chrome 和 Edge，兼容桌面和移动宽度。
- 首屏静态资源使用长期缓存，运行配置禁止缓存。
- 满足键盘操作、语义标签、焦点可见、减少动画和基本对比度要求。
- 模型内容按不可信纯文本渲染，禁止脚本和富文本注入。
- 未认证用户先跳转公司统一认证页面；一期不建设本系统登录页，是否使用自有登录页后续再处理。身份由公司统一登录或网关注入，前端不保存身份令牌。
- 页面渲染异常不得直接形成空白屏幕，应展示可理解的恢复提示；已经提交的问题仍以后端持久化状态为准。
- 会话消息加载期间禁止提交新问题；快速切换多个会话时只允许最后一次选择的响应更新消息区。

## 第一阶段验收

- 核心问答路径在成功、失败、停止、重生成、断线恢复场景下行为明确。
- 左侧会话按本地自然日正确显示“今天、昨天、更早”，跨月、跨年和无效时间数据不会导致会话丢失。
- 修改名称、删除确认、点击外部关闭、`Esc` 关闭、发送后页面稳定、会话快速切换和错误降级均有组件回归测试及真实浏览器验收。
- 统一工作台的桌面与窄屏并列侧栏、侧栏默认展开及折叠恢复、输入区 Agent 选择、欢迎页、执行过程/结果分区和安全 Markdown 表格均完成验收。
- 执行过程五阶段状态、流程版本、意图摘要、两个通道的返回数量、证据核验结论、耗时、失败/停止状态可实时展示并在刷新后恢复；页面不暴露提示词、思维链或原始 SQL。
- 证据冲突可见且不会被普通回答样式掩盖。
- 页面刷新后，已完成答案可以通过后端消息接口恢复。
- 输入区和拖拽区域均不提供文件上传能力，问题请求不携带 `files` 字段。
- 仅新建会话展示 Agent 选择、首问固化类型、后续请求省略类型、首问建会话、游标续页、虚拟滚动、执行过程/产物恢复和活动会话删除保护均有自动化回归。
- 所有自动化质量门禁通过，容器以非特权用户运行。
- HTTP 网关测试证明每个普通接口和 SSE 请求只使用 GET 或 POST、所有操作 URL 唯一，并与后端动作路径契约一致。

## 待确认项

- 服务正式名称、品牌素材和视觉规范。
- 公司统一认证的跳转地址、协议、回跳参数、会话失效处理、权限模型和开发环境测试身份；是否建设本系统登录页不属于一期。
- 知识库引用信息结构、业务数据库证据结构和冲突详情结构。
- 敏感数据分级、脱敏、审计和答案留存期限。
- 公司大模型错误码、流式中断和取消语义的最终契约。
- 测试、生产域名，网关策略与监控平台。
