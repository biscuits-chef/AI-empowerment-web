---
name: ui-accessibility
description: 用于页面、组件、样式和交互修改，检查可访问性、响应式和视觉一致性。
---

# 界面与可访问性

1. 以业务任务为主线，保持对话界面简洁，不复制第三方品牌资产。
2. 使用语义元素、可读标签、焦点可见、合理标题层级和实时区域。
3. 所有操作支持键盘；颜色不能是唯一状态信号。
4. 检查 320 像素移动宽度、常见桌面宽度、深色和浅色主题。
5. 支持系统减少动画偏好，避免无意义动画和布局跳动。
6. 使用真实浏览器验证核心交互，并保留自动化组件测试。

模型文本不得通过原始 HTML 注入页面。
