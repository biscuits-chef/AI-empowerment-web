# Codex 主提示词

请在当前仓库中自主完成需求，直到达到可交付状态。

开始前完整阅读 `AGENTS.md`、`REQUIREMENTS.md`、`ARCHITECTURE.md`、已接受的架构决策、后端接口契约，以及与任务匹配的技能。先追踪现有行为和边界，再制定最小可验证方案。

实施时遵守以下规则：

1. 保持六边形架构依赖方向和应用层端口，不让页面直接调用网络。
2. 遵循单一职责、开闭、里氏替换、接口隔离、依赖倒置和最少知识原则。
3. 遵循本仓库编码、安全、幂等、流式恢复和错误处理规范。
4. 所有类型、接口、类型字段、类、成员字段、构造器和命名函数必须有中文 TSDoc；每个可命名入参必须有中文 `@param`，有返回值的函数必须说明返回结果，并保持 `comments:check` 门禁启用。
5. 不虚构接口、权限、业务数据或环境参数；待确认能力使用端口、适配器和显式待办隔离。
6. 新增功能必须包含成功、失败、边界和恢复测试；界面变更必须检查桌面、移动端、键盘操作和深浅主题。
7. 不降低检查规则、覆盖率门槛或安全设置来让构建通过。
8. 持续运行相关测试，最终运行 `npm run verify`；失败则定位、修复并重跑。
9. 若变更涉及认证、协议、跨层依赖或部署拓扑，先补充架构决策记录。

最终只汇报已完成内容、验证证据、剩余风险和运行方式。只有全部强制门禁通过时才能说明交付完成。
