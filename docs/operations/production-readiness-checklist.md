# 生产就绪检查表

## 需求与责任

- [ ] 服务正式名称、负责人、值班和升级路径已确认。
- [ ] 第一阶段范围、验收人和非目标已签字确认。
- [ ] 知识库、业务数据库、大模型和前端的责任边界已确认。

## 身份与安全

- [ ] 公司统一认证跳转与回跳已接入，后端不接受客户端自报用户身份；一期不建设自有登录页。
- [ ] Cookie 使用安全、仅 HTTP、合理同站属性；会话过期行为已验证。
- [ ] 接口权限、对象归属和越权测试通过。
- [ ] 数据分级已完成，“不涉及敏感数据”的结论已有业务和安全确认。
- [ ] 问题、答案、引用、日志和审计的脱敏与保留期限已确定。
- [ ] 内容安全策略已在真实域名验证，无不必要的放宽。
- [ ] 依赖漏洞和镜像漏洞扫描通过。

## 正确性与恢复

- [x] 会话重命名、删除确认、点击外部关闭和 `Esc` 关闭已使用页面组件测试覆盖，不依赖原生浏览器弹窗；会话操作菜单所属虚拟行会提升层级，修改名称与删除对话框进入浏览器顶层，后续会话标题及悬停滚动不会覆盖操作层。
- [x] Agent 选择器仅在新建会话展示，首问提交类型后已有会话隐藏并锁定选择，后续请求不发送 `agentType`；首问建会话、稳定游标续页、会话列表虚拟滚动和活动会话删除提示已实现。
- [x] 飞书原型中的统一工作台、桌面/窄屏并列侧栏、侧栏默认展开及折叠恢复、欢迎任务入口、执行过程/结果分区和安全 Markdown 表格已有自动化覆盖，并完成 715px 与 390px 真实页面检查。
- [x] 历史消息可恢复执行阶段、来源/产物、人工复核和真实回答状态，活动回答会尝试继续跟随。
- [ ] 多副本下 Redis Stream 即时续流、进程重启后的生成任务续跑和长时间网络异常恢复已完成环境验收。
- [x] 发送后的严格模式渲染、错误边界降级、会话加载禁用和快速切换竞态已使用自动化回归覆盖。
- [x] 一期输入区未展示文件上传入口，问题接口拒绝非空文件引用，文件控制器默认不注册。
- [ ] 后续阶段重新立项时，再验收 OBS、安全扫描、Excel 解析、扫描 PDF/图片 OCR 和附件生命周期；本项不阻塞一期上线。
- [ ] 双通道冲突详情接口已确认，人工复核提示可追溯到证据。
- [ ] 真实产物的预览/下载授权契约已确认；当前引用标识不得当作可下载产物。
- [ ] 意图识别、无证据、冲突、模型超时和拒答场景已验收。
- [ ] 正常答案持久化并能在刷新后恢复。
- [ ] 断线重连、事件缺口、重复提交、重复取消和重新生成已验证。
- [ ] 停止生成能取消后端和模型侧任务，不只是关闭浏览器连接。
- [ ] 上下文窗口、裁剪、会话隔离和防幻觉策略已由后端验证。

## 性能与可用性

- [ ] 10 QPS 提交量和流式连接规模已联合压测。
- [ ] 浏览器、网关、Jetty 和模型超时关系已确认。
- [ ] 静态资源缓存、运行配置不缓存和回滚策略已验证。
- [ ] Chrome、Edge、桌面和移动宽度完成验收。
- [ ] 键盘、焦点、对比度和减少动画检查通过。

## 运维与发布

- [ ] 开发、测试、生产 XML 配置由发布平台注入且不含密钥。
- [ ] 容器以非特权用户、只读文件系统和最小能力运行。
- [ ] 健康检查、前端错误、接口失败率、流中断率和用户反馈可监控。
- [ ] 关键操作关联后端跟踪号，审计查询路径已演练。
- [ ] 灰度、快速回滚、配置回滚和故障演练已完成。
- [ ] `npm run verify` 和镜像构建在持续集成中强制执行。

未全部勾选前，不得将系统描述为已具备生产上线条件。
