# AI 赋能项目前端架构说明

> 本文描述第一阶段前端目标架构，并区分当前已实现能力与后续建设项。统一工作台交互以[飞书交互原型](https://my.feishu.cn/wiki/HC7Dwym3Vi2jbikwnq6cMqhhnUf?from=from_wechat)、已归档的《产品需求文档》及后续确认结论为基线；输入区第一阶段默认且仅开放“智能问数”，其他 Agent 只展示入口。

## 1. 架构定位

前端采用轻量六边形架构。React 属于展示层，业务流程由应用层用例和状态机编排，远端能力通过 `QaGateway` 等端口访问，HTTP、SSE 和运行时 XML 配置是可替换适配器。

```text
展示组件
   ↓
应用用例、页面状态机与权限判断
   ↓
前端领域类型和交互不变量
   ↑
接口端口 ← HTTP、SSE、上传、XML 配置适配器
```

前端不复制后端完整领域模型，只保存渲染和交互需要的最小状态。GoldenDB 中的会话、回答、文件、证据和权限状态始终是最终事实来源。

## 2. 一期页面范围

第一阶段页面采用“固定侧栏 + 顶部上下文 + 主工作区 + 底部任务输入区”的统一工作台结构，包括：

- 当前用户与认证状态初始化。
- Agent 仅由输入区的轻量下拉菜单选择，侧栏不重复展示 Agent 导航。
- 会话列表、新建会话、删除会话和修改会话名称。
- 智能问答聊天区、流式回答、引用、证据冲突和人工复核提示。
- 临时文件上传、Excel/文本/OCR 解析状态、用途确认、删除和随问题引用。
- 回答复制、喜欢/不喜欢、重新生成和停止生成。
- 可展开执行过程、安全 Markdown 结果以及独立来源/产物区域。

Agent 列表由前端本地配置名称、稳定类型、顺序、图标和开放状态，不请求后端目录接口。用户选择“智能问数”时进入聊天，并在每次提问的 JSON 请求中携带 `agentType=SMART_DATA`；选择其他 Agent 时不创建业务会话、不提交问题，只弹出固定提示：`该功能尚未开放`。后端必须独立执行类型和权限校验，不能把前端配置当作安全依据。

空白态把主输入区置于欢迎语和快捷任务之间，快捷任务只更新草稿。桌面端侧栏常驻；窄屏侧栏转为有遮罩的抽屉，并复用相同会话和用户组件，避免形成两套行为。Agent 选择统一收敛到桌面端与窄屏共用的输入组件。

## 3. 认证启动流程

```text
加载同源运行时配置
        │
        ▼
请求当前用户接口
        │
   ┌────┴────┐
   ▼         ▼
认证成功    401/会话失效
   │         └─> 跳转或提示重新登录
   ▼
读取前端 Agent 配置并加载会话列表
```

- 未认证时先跳转公司统一认证页面；一期不实现自有登录页。正式环境使用公司统一认证产生的安全 Cookie 或网关会话，浏览器不保存长期访问令牌。公司跳转地址、协议与回跳参数未确认前只保留边界，不虚构实现。
- 开发环境可使用后端模拟用户，但页面必须显示醒目的“模拟用户/开发环境”标识。
- 前端传入的用户 ID 只能作为非可信展示或开发参数，后端身份始终以认证上下文为准。
- 测试和生产构建不得提供绕过统一认证的用户切换入口。

## 4. 会话与消息流程

1. 启动后按稳定游标分页加载会话列表；展示层以会话 `updatedAt` 和浏览器本地自然日边界划分“今天、昨天、更早”，保持服务端顺序且隐藏空分组。无法解析的时间归入“更早”，避免会话丢失；现有会话提交问题或重新发起成功后，应用层使用回答受理时间更新本地 `updatedAt`，使会话立即移动到“今天”。
2. 选择会话后加载已持久化消息和回答快照。
3. 新聊天第一次有效提问时先创建会话，再提交问题。
4. 提交接口返回 `answerId`、`traceId` 和流地址后建立 SSE 连接。
5. 提交前复制当前附件元数据；问题发送后立即把问题与附件卡片作为同一条乐观用户消息展示，上传列表随后可以安全清空。
6. 页面刷新或重新进入会话时以服务端消息、回答快照和消息内附件状态恢复，不依赖浏览器临时状态。
7. 重新生成成功后，前端从当前消息中找到该回答之前最近的用户问题，把服务端返回的新 `questionId` 用作新用户消息 ID，复制问题文本和附件展示快照，再追加新回答并订阅流。后端负责重新执行完整链路；旧问题和旧答案继续保留。
8. 删除前弹出确认框；若后端返回活动会话 `409`，关闭确认框并提示“会话正在执行，请停止后删除”。前端不把本地 SSE 是否存在当作删除依据，也不自动发起停止。

会话列表保存 `nextCursor` 和 `hasMore`。滚动接近底部时只请求下一页并按 ID 去重；列表使用固定高度虚拟行，只挂载可见区和预加载缓冲。分组标题也是虚拟行，不能因为分页而重复或打乱“今天、昨天、更早”。

会话、回答和文件请求都携带同源凭据。401 提示重新登录；403 使用不泄露资源信息的无权提示；429 展示稍后重试及服务端允许的等待时间。

## 5. 临时文件交互

目标文件交互流程：

```text
选择文件
   │
   ▼
前端预校验类型、大小和数量
   │
   ▼
上传 -> 扫描/Excel结构化解析/文本解析/OCR -> READY
                  └────> FAILED（允许删除或重新上传）
   │
   ▼
用户提问携带文件引用（fileId + usage）
```

- 一期单文件不超过 1 MiB；支持 XLSX、文本 PDF、扫描 PDF、DOCX、TXT、MD 和常见图片，最终类型和单次数量以后端配置为准。
- 临时文件存在两种用途：`QUERY_INPUT` 表示查询条件/问题补充，`EVIDENCE` 表示回答证据；页面可使用 `AUTO` 让系统识别，但识别不确定时必须让用户确认用途。
- 例如 Excel 中包含一批产品编号时，页面应显示识别出的工作表、有效编号数量、重复/无效行和批量上限；它只作为查询输入，不能显示为答案证据。
- 支持文本解析、Excel 结构化解析、扫描 PDF 和图片 OCR。前端只展示处理阶段和安全错误，不执行可信解析或 OCR。
- 文件未达到 `READY` 时禁止随问题提交，并明确显示仍在解析或 OCR。
- 问题只提交后端文件 ID 和用途 `AUTO|QUERY_INPUT|EVIDENCE`，不提交 OBS 地址、对象键或签名 URL。
- 批量查询结果按产品编号等输入项逐项展示成功、无权限、未找到和失败；不得把部分失败包装为整批成功。
- 前端不得展示或长期缓存供 HiAgent 使用的短期 OBS URL。
- 删除文件前提示影响；已经被回答引用的文件按后端规则进入延迟清理，前端不能假设立即物理删除。
- 发送成功后，附件从输入区转移到对应用户消息卡片，展示文件名、大小、本次用途和状态。这里的“转移”只改变页面状态，不重新上传或复制对象。
- 历史消息中的附件由后端 `attachments` 字段恢复；`DELETE_PENDING` 展示“文件已过期”，但原问题和元数据继续保留。消息区不接收对象键、OBS URL 或本地路径，也不自行构造下载地址。

## 6. 问答与结构化流事件

目标流程：

```text
问题 + READY 文件引用（fileId + usage）
        │
        ▼
提交成功并显示用户消息/回答占位
        │
        ▼
SSE：metadata -> understanding -> retrieval -> reconciliation
        │
   ┌────┴────────────┐
   ▼                 ▼
clarification      generation_started
   │                 │
确定性追问        citation/evidence/conflict/delta
                     │
                     ▼
             completed/error/cancelled
```

前端根据事件类型更新结构化状态，不从自然语言中猜测阶段：

- `workflow_plan_selected`：展示后端实际选定的查询场景和计划版本。
- `delta`：只追加当前回答文本。
- `intent_recognized`：展示受控意图名称、置信度和已确认查询条件数量，不展示模型内部推理。
- `knowledge_retrieval_completed` / `business_query_completed`：分别展示授权知识片段和结构化业务事实数量。
- `evidence_reconciliation_started` / `evidence_assessed`：展示双通道核验过程、结论和冲突字段数量。
- `generation_completed`：表示回答内容已经生成并等待最终持久化确认；只有 `completed` 才表示完整回答已保存。
- `query_input`：展示从文件提取并被采用的查询参数摘要、有效数量和错误行，但不加入证据引用。
- `citation` / `evidence`：保存真正事实来源的类型、标题、字段、时间和可展示定位。
- `conflict`：并列展示知识库、GoldenDB、临时文件中存在差异的字段和值。
- `manual_review_required`：显示醒目的人工复核提示，不被后续文本覆盖。
- `clarification_required`：展示确定性追问；用户下一条回答作为补充参数提交。
- `completed`、`cancelled`、`failed`、`incomplete`：进入对应终态。

如果用户补充的信息仍不足，继续展示追问；如果用户明确提出新问题，页面按服务端决策结束旧追问上下文并执行新问题。前端不自行做指代消解或选择歧义候选，只负责把用户选择的稳定候选值提交给后端。

## 7. 页面状态机

回答页面状态建议与后端语义对齐：

```text
IDLE -> SUBMITTING -> UNDERSTANDING -> RETRIEVING -> GENERATING
                   └─> NEEDS_CLARIFICATION
                   └─> CANCEL_REQUESTED -> CANCELLED / CANCEL_FAILED
                   └─> COMPLETED / FAILED / INCOMPLETE
```

文件状态建议为：

```text
LOCAL -> UPLOADING -> UPLOADED -> SCANNING -> PARSING/OCR_PROCESSING -> READY
               └───────────────────────────────────────────────> FAILED
```

状态迁移必须由服务端响应和事件驱动。重复点击提交、停止、反馈和重新生成时，按钮应立即进入处理中状态并依赖幂等接口避免重复操作。

页面交互还必须满足以下不变量：

- 会话操作菜单由组件状态管理，点击菜单外部或按 `Esc` 后关闭。
- 修改名称和删除确认采用页面内 `dialog` / `alertdialog`，不使用不同运行容器支持度不一致的浏览器原生 `prompt` / `confirm`。
- 会话切换请求使用递增序号隔离；过期响应不得覆盖最后选择的会话，加载期间输入区保持禁用。
- React 副作用只能返回清理函数或不返回值；滚动、聚焦等浏览器方法的返回值不得透传为副作用清理结果。
- 根组件使用错误边界兜底未处理的渲染异常，展示刷新恢复入口，禁止直接留下白屏；该兜底不替代根因修复和异常监控。
- 会话虚拟行中的图标是不可压缩的固定尺寸元素，标题是独立的单行滚动视口；应用层用首次有效提问前 100 个字符生成默认名称，展示层按真实溢出距离和固定像素速度计算动画时长。悬停只移动标题文本，不改变虚拟行高度和宽度；顶部标题不使用滚动效果，通过弹性收缩与单行省略为不可压缩的操作区让出空间。

## 8. 停止、断线与恢复

- 点击“停止生成”调用停止接口，不以关闭 SSE 连接代替停止。
- 停止请求后页面进入 `CANCEL_REQUESTED`，继续接收服务端最终状态。
- `CANCELLED` 时保留已经收到且服务端已持久化的部分答案，并明确标记“已停止，内容可能不完整”。
- 浏览器切换页面、网络断开或刷新不默认取消后端生成。
- 重连携带最后事件 ID；事件连续时继续追加。
- 服务端报告重放缺口时，清空不可信的本地拼接结果，读取回答快照恢复，绝不静默展示残缺答案。
- SSE 终止但未收到终态时，先查询回答快照，再决定重连或显示恢复提示。
- 历史消息中的 `executionEvents` 恢复已完成阶段和人工复核标识，`artifacts` 恢复来源/产物。若历史状态仍为活动态，先读取回答快照，再使用持久事件 ID 继续订阅；恢复过程中不得重复追加已确认文本。

## 9. 证据展示与准确性体验

答案正文、引用和证据对照分开建模。建议的证据卡片至少展示：

- 来源类型：知识库、GoldenDB，以及具有 `EVIDENCE` 用途的临时文件。
- 来源标题或业务记录标识。
- 关键字段和值。
- 文档版本、业务生效时间或数据时间。
- 可安全展示的页码、片段或记录定位。

冲突不是普通警告文本，而是结构化对照视图。同一字段存在不同值时同时展示各来源值、来源时间和“请人工复核”，不默认突出某一方为正确。`QUERY_INPUT` 文件只显示在“查询条件”区域，绝不进入来源卡片或冲突对照；证据不足时明确说明无法确认，不使用流畅措辞掩盖无依据回答。

执行过程采用五阶段视图模型，将持久化事件折叠为问题理解、知识库检索、GoldenDB 查询、证据核验和回答生成。阶段模型只接收事件稳定编码、数量、状态、时间和流程版本；原始问题、知识正文、业务数据、系统提示词、思维链、SQL、内部地址与密钥均不进入阶段摘要。旧历史事件缺少完成事件时，可由后续阶段已经开始推断前一阶段已结束，但不得据此伪造结果数量。

## 10. 安全边界

- 浏览器运行时配置不包含密钥、数据库地址、模型令牌或 OBS 凭据。
- 所有请求默认携带同源安全 Cookie；跨域策略由网关和后端统一控制。
- 模型回答、知识片段、OCR 内容和文件名按纯文本渲染，不解释不可信 HTML。
- 回答正文通过自有受控 Markdown 渲染器展示标题、列表、表格、代码、加粗和行内代码；不得使用 `dangerouslySetInnerHTML`，不支持的 HTML 原样作为文本显示。
- 下载、预览和引用链接必须由后端授权并短期有效，禁止使用任意外链。
- 页面日志、埋点和错误上报不记录完整问题、回答、业务字段、OCR 文本或签名 URL。
- Nginx 设置内容安全策略、禁止嵌入、类型嗅探保护和最小权限策略。
- 上传错误只展示安全、可操作的原因，不泄露对象键、内部路径或外部平台响应正文。

## 11. 可用性与性能

- 普通请求设置总超时，上传显示进度，流式请求使用空闲超时与心跳判断。
- 同一会话中可限制同时生成数量，避免用户误触造成重复消耗。
- 长会话使用游标分页和消息虚拟化，不能一次加载全部历史。
- 文件状态采用有界轮询或服务端事件，页面不可无限高频刷新。
- 50 个并发用户场景下验证首屏、会话切换、50 条 SSE 连接、重连和停止交互。
- 所有错误提供用户可理解的下一步，同时保留 `traceId` 供支持人员定位。

## 12. 当前实现与目标差距

当前前端已具备的基线包括：轻量分层、飞书原型风格的统一工作台、桌面/窄屏侧栏、前端本地 Agent 配置与输入区选择、提问 `agentType` 参数、欢迎任务入口、会话与消息界面、稳定游标续页、会话列表虚拟滚动、今天/昨天/更早分组、首问创建并落库、页面内重命名/删除对话框、活动会话删除错误提示、操作菜单外部关闭、会话切换竞态保护、页面错误边界、两步式问答、可展开执行过程、受控 Markdown 结果、来源/产物区域、刷新后的执行过程/来源产物恢复与活动回答继续跟随、输入区和消息内停止入口、复制、反馈、把原问题重新发起为新问答轮次、冲突提示占位、临时附件点击/拖拽上传、用途选择、删除、问题引用、用户消息附件即时展示与历史恢复，以及 XML 运行时配置。

以下仍为目标能力，尚不能视为完成：

- 公司统一认证真实跳转/回跳流程、模拟用户环境标识与严格环境隔离；一期不建设自有登录页。
- 公司权限驱动的后端 Agent 功能授权；前端目录及开放状态为本地固定值，不能替代后端权限校验。
- 文件处理状态轮询、Excel 查询输入解析结果、扫描 PDF/图片 OCR 进度、刷新后恢复未提交附件，以及已引用文件的授权预览/下载管理。
- 多来源结构化证据卡片、查询输入与证据隔离、字段级冲突对照和可追溯引用。
- 后端授权的 Word、Excel、PPT 等真实产物预览和下载；当前契约只有引用标识，页面不会伪造地址。
- 完整的歧义候选选择、追问补参恢复和跨刷新状态恢复。
- Redis Stream 跨实例即时续流和异常网络测试；GoldenDB 历史执行事件、来源产物和快照恢复已接入。
- 10 QPS/50 并发联调、无障碍、安全和浏览器兼容性验收。

## 13. 与后端迭代的对齐顺序

1. 对齐统一认证、当前用户和 Agent 契约。
2. 对齐会话游标、消息快照和回答版本契约。
3. 对齐文件上传、用途识别、Excel 查询输入、处理状态、文件引用和删除契约。
4. 对齐结构化 SSE、停止、快照恢复和幂等错误语义。
5. 对齐证据项、引用、冲突与人工复核展示。
6. 完成网络中断、刷新恢复、重复提交、停止竞争和文件失败场景测试。
7. 完成 50 并发用户体验、安全检查和生产配置验收。

后续合同比对、申赎确认单核对和合同智能审核应以独立页面模块和用例接入；一期只展示未开放 Agent，不提前引入这些业务流程。
