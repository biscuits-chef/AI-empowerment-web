# Codex 前端工程守则

## 工作目标

在尽量少人工干预的前提下交付可验证、可回滚、可观测的企业级前端。不得虚构接口、身份权限、业务规则或生产环境参数；遇到待确认项时，用显式端口和配置隔离，并在文档中登记。

## 必读顺序

1. `REQUIREMENTS.md`
2. `ARCHITECTURE.md`
3. `docs/adr/` 中已接受的决策
4. 与任务匹配的 `.agents/skills/*/SKILL.md`
5. 后端接口契约 `docs/api/backend-contract.md`

## 架构约束

- 依赖方向必须是 `presentation → application → domain`；基础设施通过应用层端口接入。
- 页面组件不得直接发起网络请求，不得直接解释后端状态码。
- 领域层不得依赖 React、DOM、HTTP 或具体状态库。
- 每个模块只承担一个清晰职责；通过小接口扩展能力，禁止建立万能服务和全局可变状态。
- 使用组合和依赖倒置，不为“可能有用”而提前引入抽象。
- 流式协议、错误映射、运行配置均应有独立适配器和单元测试。

## 编码规范

- TypeScript 必须开启严格模式，禁止 `any`、非必要类型断言和未处理的异步任务。
- 所有类型、接口、类型字段、类、成员字段、构造器和命名函数都必须有中文 TSDoc；每个可命名入参必须有对应的中文 `@param`，有返回值的函数必须用中文说明返回结果。
- `npm run comments:check` 为只读强制门禁，扫描生产与测试源码；匿名回调不要求独立声明注释，解构入参使用位置标签并由对应属性类型逐字段说明，禁止关闭检查。
- 注释必须与实现同步，禁止逐行复述代码、保留注释掉的旧代码或用注释替代清晰命名；协议字段和第三方术语可保留英文，并用中文说明业务含义。
- 命名表达业务含义；单个函数优先控制在 50 行以内，复杂分支拆成可测试的小函数。
- 不使用 `dangerouslySetInnerHTML` 展示模型内容；模型输出默认视为不可信文本。
- 不在浏览器存储令牌、敏感问题或完整答案；认证优先使用同源安全 Cookie。
- 所有写操作必须处理重复点击和幂等；问题提交、重生成、取消必须携带独立幂等键。
- 用户可见文案使用中文；日志和错误不得暴露堆栈、密钥、内部地址或原始服务异常。
- 遵循阿里规范的共同原则：清晰命名、显式边界、常量化、异常闭环、输入校验、最小权限和可测试性。

## 流式回答不变量

- 正常生成内容由服务端持久化，前端只负责展示，刷新后从历史消息恢复。
- 停止生成必须调用取消接口；仅关闭流连接不代表生成已停止。
- 重新生成追加答案版本，不覆盖原答案。
- 断线重连携带最后事件序号；收到事件缺口响应后读取答案快照。
- `NEEDS_CLARIFICATION`、`COMPLETED`、`INCOMPLETE`、`CANCELLED`、`CANCEL_FAILED`、`FAILED` 是终态；`completed` 事件值为 `clarification_required` 时保持追问终态。
- 双通道证据不一致时必须展示两个通道的证据与人工复核提示，不得替用户裁决。

## 交付门禁

完成修改后必须依次运行：

```bash
npm run format:check
npm run comments:check
npm run lint
npm run typecheck
npm run test:coverage
npm run build
```

优先运行 `npm run verify`。任何失败均不允许宣称完成。涉及布局时必须在桌面和移动宽度做真实浏览器检查；涉及接口时必须覆盖成功、鉴权失败、超时、服务端错误、取消和重放缺口。

## 变更纪律

- 修改跨层依赖、认证、流式协议、运行配置或部署拓扑前，新增或更新架构决策记录。
- 不修改无关代码，不删除用户已有变更，不绕过质量阈值。
- 新依赖必须说明必要性、安全影响、维护状态和替代方案。
- 完成时说明变更、验证结果、未解决风险和回滚方式。
